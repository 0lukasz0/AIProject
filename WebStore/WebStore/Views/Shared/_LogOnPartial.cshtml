@if(Request.IsAuthenticated) {
    @Html.ActionLink(User.Identity.Name+" [Log Off]", "LogOff", "Account")
}
else {
     @Html.ActionLink("Log On", "LogOn", "Account", new { returnUrl = ViewBag.ReturnUrl }, null)
}
